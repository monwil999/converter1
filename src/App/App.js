import React, { useState } from "react";
import Form from "../Form";

function App() {
    <div className="app">
      <Clock />
      <Form
        calculateResult={calculateResult}
        result={result}
        ratesData={ratesData}
      />
    </div>
};

export default App;
